# Keep npm fully self-contained inside this project directory.
cache=./.npm-cache
# React 19 + Next 14 peer-dep mismatch in package.json; the bundler's own
# npm install (firebase deploy) must not fail on ERESOLVE.
legacy-peer-deps=true
